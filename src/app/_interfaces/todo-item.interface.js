class TodoItem {
  construction({
    title,
    description,
    isComplete,
    dueDate
  }) {
    this.title = title;
    this.description = description;
    this.isComplete = isComplete;
    this.dueDate = dueDate;
  }
}
